import { ExpoConfig } from '@expo/config-types';
import { ConfigPlugin, ModPlatform } from '../Plugin.types';
export declare function wrapWithWithDeprecationWarning<T>({ plugin, platform, packageName, unversionedName, updateUrl, shouldWarn, }: {
    plugin: ConfigPlugin<T>;
    updateUrl: string;
    platform: ModPlatform;
    packageName: string;
    unversionedName: string;
    shouldWarn: (config: ExpoConfig) => boolean;
}): ConfigPlugin<T>;
