import { ModPlatform } from '@expo/config-plugins';
export default function configureIOSProjectAsync({ projectRoot, platforms, }: {
    projectRoot: string;
    platforms: ModPlatform[];
}): Promise<void>;
